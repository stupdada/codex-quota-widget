const COMPACT_LAYOUT = Object.freeze({
  width: 500,
  hud: Object.freeze({
    collapsedHeight: 86,
    expandedHeight: 168
  }),
  topStrip: Object.freeze({
    collapsedHeight: 28,
    expandedHeight: 124
  }),
  scale: Object.freeze({
    min: 0.46,
    max: 1.25
  })
});

module.exports = { COMPACT_LAYOUT };
